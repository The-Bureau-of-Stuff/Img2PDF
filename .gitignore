# Build outputs
bin/
obj/
x64/
x86/
ARM64/
AppPackages/
BundleArtifacts/
*.appx
*.msix
*.appxsym
*.msixsym

# Per-dev-machine signing cert — never shared/committed (see M5 plan)
*.pfx

# Generated icon/logo artifacts — reproducible from tools/generate-icons.py, not source
tools/out/

# IDE
.vs/
.idea/
*.user
*.suo

# OS
Thumbs.db
.DS_Store

# Internal planning docs — own private repo (github.com/The-Bureau-of-Stuff/Img2PDF-docs),
# nested here as a separate git checkout so CLAUDE.md's docs/... paths still resolve locally.
# Not a submodule reference on purpose: this repo carries zero trace of it.
docs/
